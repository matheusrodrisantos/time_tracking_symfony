{% extends 'base.html.twig' %}

{% block title %}Departament{% endblock %}

{% block body %}
    <h1>Departament</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departament.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ departament.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_departament_index') }}">back to list</a>

    <a href="{{ path('app_departament_edit', {'id': departament.id}) }}">edit</a>

    {{ include('departament/_delete_form.html.twig') }}
{% endblock %}
