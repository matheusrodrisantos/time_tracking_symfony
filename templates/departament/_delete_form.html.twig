<form method="post" action="{{ path('app_departament_delete', {'id': departament.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departament.id) }}">
    <button class="btn">Delete</button>
</form>
