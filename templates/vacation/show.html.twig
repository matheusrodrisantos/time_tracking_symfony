{% extends 'base.html.twig' %}

{% block title %}Vacation{% endblock %}

{% block body %}
    <h1>Vacation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vacation.id }}</td>
            </tr>
            <tr>
                <th>Initial_date</th>
                <td>{{ vacation.initialDate ? vacation.initialDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Final_date</th>
                <td>{{ vacation.finalDate ? vacation.finalDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vacation_index') }}">back to list</a>

    <a href="{{ path('app_vacation_edit', {'id': vacation.id}) }}">edit</a>

    {{ include('vacation/_delete_form.html.twig') }}
{% endblock %}
